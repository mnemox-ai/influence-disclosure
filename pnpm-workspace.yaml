packages:
  - 'packages/*'

# esbuild ships a platform binary and its install script places it. It arrives only as a
# transitive dependency of vitest; nothing published from this repo uses it.
allowBuilds:
  esbuild: true
