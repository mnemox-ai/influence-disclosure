{
  "name": "influence-disclosure-workspace",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.25.0",
  "engines": {
    "node": ">=22.13"
  },
  "license": "Apache-2.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run --coverage",
    "typecheck": "tsc -b --pretty false",
    "spec:types": "node scripts/gen-types.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@vitest/coverage-v8": "^3.2.4",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
