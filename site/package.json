{
  "name": "influence-disclosure-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "wrangler pages deploy ./dist --project-name=influence-disclosure --branch=main",
    "fonts": "python ../tools/subset_fonts.py"
  },
  "dependencies": {
    "@fontsource-variable/geist": "^5.2.5",
    "astro": "^5.14.1",
    "influence-disclosure": "workspace:*"
  },
  "devDependencies": {
    "@fontsource/noto-sans-tc": "^5.3.0"
  }
}
